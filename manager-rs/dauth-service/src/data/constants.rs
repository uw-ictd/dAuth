/// All constants related to auth vector data types
pub mod vector_data {
    pub const ID_LENGTH: usize = 7;
    pub const K_LENGTH: usize = 16;
    pub const OPC_LENGTH: usize = 16;
    pub const SQN_LENGTH: usize = 6;
}
